#!/bin/sh

echo "🔍 Running Next.js type and lint checks..."
if ! npm run lint 2>&1 | grep -q "error"; then
  echo "✅ All checks passed!"
  exit 0
fi

echo "❌ Errors found. Fix them before committing."
npm run lint
exit 1
